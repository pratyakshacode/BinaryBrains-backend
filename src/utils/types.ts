/**
 * File contains the various types to be used all over the application
 */

export enum USER_ROLE {
    admin = "admin",
    student = "student",
    creator = "creator"
}

export const USER_ROLES_ARRAY = ['admin', 'creator', 'student'];